[build]
  command = "npm run build"
  publish = "dist"
  environment = { SECRETS_SCAN_OMIT_KEYS = "AIRTABLE_WEBHOOK_URL,NODE_ENV,PORT" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200